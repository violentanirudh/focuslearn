const express = require('express')
const router = express.Router()

router.post('/signin', () => {})
router.post('/signup', () => {})
router.post('/signout', () => {})
router.post('/verify', () => {})
router.post('/reset', () => {})

module.exports = router