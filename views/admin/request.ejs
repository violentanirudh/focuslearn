<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>
<body class="bg-gray-100 font-sans">
    <div class="flex">
        <%- include('./partials/sidebar') %>

        <main class="flex-1 p-8">
            <div class="max-w-4xl mx-auto">
                <header class="mb-8">
                    <div class="flex items-center justify-between">
                        <h1 class="text-3xl font-bold text-gray-800">Course Requests</h1>
                        <span class="px-3 py-1 text-sm font-medium text-blue-800 bg-blue-100 rounded-full">
                            Pending
                        </span>
                    </div>
                    <p class="mt-2 text-sm text-gray-600">Playlist ID: <%= playlist.playlistId %></p>
                </header>

                <div class="flex w-full gap-6 items-start">
                    <div class="w-3/5 bg-white rounded-lg shadow-md">
                        <div class="p-6 border-b border-gray-200">
                            <h2 class="text-xl font-semibold text-gray-800">Playlist Details</h2>
                        </div>
                        <div class="p-6">
                            <ul class="space-y-4">
                                <li class="flex justify-between">
                                    <span class="text-gray-600">Requested By</span>
                                    <span class="font-medium text-gray-800"><%= playlist.requestedBy.fullName %></span>
                                </li>
                                <li class="flex justify-between">
                                    <span class="text-gray-600">Email</span>
                                    <span class="font-medium text-gray-800"><%= playlist.requestedBy.email %></span>
                                </li>
                                <li class="flex justify-between">
                                    <span class="text-gray-600">Total Videos</span>
                                    <span class="font-medium text-gray-800"><%= playlist.totalVideos %></span>
                                </li>
                                <li class="flex justify-between">
                                    <span class="text-gray-600">Channel</span>
                                    <span class="font-medium text-gray-800"><%= playlist.channel %></span>
                                </li>
                                <li class="flex justify-between">
                                  <span class="text-gray-600">Last Activity</span>
                                  <span class="font-medium text-gray-800"><%= new Date(playlist.lastActivity).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                              </li>
                            </ul>
                        </div>
                    </div>

                    <div class="bg-white rounded-lg shadow-md w-2/5">
                        <div class="p-6 border-b border-gray-200">
                            <h2 class="text-xl font-semibold text-gray-800">Actions</h2>
                        </div>
                        <form action="" method="POST" class="p-6 space-y-4">
                            <button class="w-full px-4 py-2 text-white bg-blue-600 rounded hover:bg-blue-700 transition duration-300" name="approve">
                                Approve Request
                            </button>
                            <button class="w-full px-4 py-2 text-white bg-red-600 rounded hover:bg-red-700 transition duration-300" name="reject">
                                Reject
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.29.0/feather.min.js"></script>
    <script>
        feather.replace();
    </script>
</body>
</html>